extends = ["_clever.toml"]

[experiment]
name = "paper-5min-clever-parallel"
output_root = "runs/paper_5min/clever/parallel"
mode = "parallel"
communication = "none"
max_parallel = 12
episodes_per_task = 1
