extends = ["base.toml"]

[experiment]
name = "mobench-3min-parallel"
mode = "parallel"
communication = "none"
max_parallel = 12
episodes_per_task = 1
time_limit_seconds = 180
output_root = "runs/3min"

[pi]
timeout_seconds = 180
