extends = ["base.toml"]

[experiment]
name = "mobench-1h-mono"
mode = "mono"
communication = "none"
max_parallel = 1
episodes_per_task = 1
time_limit_seconds = 3600
output_root = "runs/1h"

[pi]
timeout_seconds = 3600

[lean]
max_concurrent_evaluations = 1
