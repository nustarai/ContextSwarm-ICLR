extends = ["base.toml"]

[experiment]
name = "mobench-3min-mono"
mode = "mono"
communication = "none"
max_parallel = 1
episodes_per_task = 1
time_limit_seconds = 180
output_root = "runs/3min"

[pi]
timeout_seconds = 180
